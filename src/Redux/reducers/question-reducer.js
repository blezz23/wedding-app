let initialState = {
    questions: [
        {id: 1, question: "text"},
        {id: 2, question: "text2"}
    ]
};

const questionsReducer = (state = initialState) => {
    return state
};

export default questionsReducer;